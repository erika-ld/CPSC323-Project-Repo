[* Test Case 3 *]
$
function find_max (a real, b real)
{
    if (a > b)
    {
        return a;
    }
    else
    {
        return b;
    }
}
$
real x, y, max;
$
scan(x, y);
max = find_max(x, .12);
print(max);
$
